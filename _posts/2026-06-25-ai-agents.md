---
title: "Building My First AI Agent: Architecture, ReAct Loop, and Lessons Learned"
date: 2026-06-24 15:10:00 +0200
categories: [AI]
math: true
tags: [AI, Agents, LLM, ReAct, Multi-Agent, RAG]
---
# Agents

## What are AI Agents?

Humans started agents by designing predefined paths between the input and the output in order to create more independence between the user and the tools: this is an AI workflow (like RAG). It follows a specific schema: an input is given, and an LLM fetches information from a document where the pathway between the LLM and the source is predefined. Then, the LLM generates a response according to the initial prompt.

An AI agent is a tool that can act autonomously in its environment. In the decision workflow, the AI agent uses an LLM as a decision-maker. Humans no longer supervise the steps; instead, based on a specific prompt, the agent can generate the required response without human supervision. The AI agent must reason, and then act: this is the ReAct framework [1].

- RAG tends for retrieval augmented generation because it searchs for relevant information in a knowledge base and it uses an LLM to produce an answer based on that information. RAG is just a type of AI workflow [1].

#### How can we use agents in a company while ensuring confidentiality and security?

Agent can be viewed as a suit of LLMs. LLMs are provided by BigTech like OpenAI, Mistral, Gemini etc.. Inside a company like Worldline, employees regularly exchange internal and confidential information. Depending on the LLM provider policy and contract with the company, employees can leak critical data into a public LLM, Attackers can manipulate an LLM by injecting malicious instructions, If agents have tools, risks include privilege escalation, RAG systems can leak sensitive data. To avoid this, at Worldline, employees must used Librechat, an open‑source, self‑hosted chat interface designed to let you use multiple AI models without depending on a commercial platform and with a designed contract depending on the company. 

## **How do AI agents work?**

AI agent workflow works by using the react loop. The react loop follows these steps: 

- Thought
- Action
- Observation
- Answer

The final step is Answer if the Observation step is not redirected to thought due to the loop method. This extra specialization and accuracy come with costs in terms of complexity [2].

### What kind of tasks are agents good for?

Agents are initially good for complex tasks which require less accuracy. It depends on the degree of autonomy given to the agent: less autonomous, semi-autonomous, and highly autonomous (ranging from steps that are pre-determined to steps that are chosen by the agent, respectively). To have a good agent, a good breakdown of tasks is required. For example: 

- To generate search terms: use an LLM and an API
- To fetch pages: use a tool
- To write a draft, use an LLM with those sources, etc. [2]

Add an intermediate AI which acts as an LLM judge to determine if the job is done correctly or not. [2]

#### How improves agent performance?

Memory is a common way to improve performance. The LLM can reload lessons learned during previous tests and keep these notes to improve efficiency. Each run will improve the quality of the task. This should not be confused with knowledge, which is a static-based reference. [2]

#### How prevents agent errors?

Then, to prevent errors, guardrails are used because they act as a quality gate between the work that is being done and the work that is actually being finalized. Another point is to make the system higher quality. [2]

There are four patterns that reliably boost quality and capability: reflection, tool use, planning, and multi-agent collaboration. [2]

- **Reflection** simply means that we don’t end the task at the first generation, because the simple fact of fixing some details and generating again always leads to better quality work.
- **Tool-use** is a set of accessible tools that the LLM can use to execute tasks. An LLM cannot execute code natively but can request a function call. In order to get the correct tools for specific situations, it’s important to define them well. They actually have two parts: the interface for the agent and the implementation code. Good tools also consider error-handling, self-recovery, and rate limiting.
- **Planning** involves the user giving the agent a toolkit of functions and letting it figure out which tools it needs to use and when. The planning loops follow these steps: give the agent access to tools, prompt it to create a plan, execute the plan step by step, and finally repeat until you’re done.
- **Multi-agent collaboration** allows a huge model to be assigned specifically to more complex tasks, while a smaller model can handle lighter tasks. The strategic insight here is to avoid having one LLM handle all general tasks by sharing the workload between specialized LLMs, enabling parallelism, and splitting up long operations. To avoid errors, it’s important to assign each agent a specific role. Between each agent, communication could be sequential (each agent waits, takes the input of the previous one, and gives the output to the following one). It’s also possible to make agents work in parallel; this could reduce latency but adds coordination difficulty. The most common pattern today is still an AI manager that brings all of the outputs of the other agents together and works with them.

# Practical test

I built my first agent for Worldline. Bridging the gap between cybersecurity protocols and my core ambition in LLM engineering, I decided to fully automate this workflow. The agent has to ingest a questionnaire from SharePoint, an email, or a Word file, retrieve each question, and output a formatted, professional response with sources and a confidence score, all while preserving the exact input format. These documents are heavy on formatting, require precise fact-checking against internal knowledge bases, and leave zero room for creative hallucination.

## The Architecture: A 4-Agent Symphony

I initially put a massive prompt into one single LLM, which obviously led to data loss and formatting destruction. Then, I added 3 sub-agents to the workflow and did some tests. I realized that without any strict specifications, the output was consuming a huge quantity of credits (token cost). I quickly understood that the model chosen for each agent, and their specific settings depending on their specialization, matter a lot.

**Last Update of the prompt: 15/07**

The project evolve and what you see is the current prompt for the project. I decided to remove all of the previous tests prompt to keep a readable blog. 

### LLM-Manager (The Orchestrator)

- **Role:** The router. It receives the user's file and triggers the sub-agents in the exact correct order using Tool Calls.
- **Model:** `gpt-4o-mini` (Fast, highly capable of tool-calling, cost-effective).

Prompt

```json
{
  "prompt": {
    "role": "Autonomous Pipeline Orchestrator",
    "purpose": "Execute a multi-agent workflow to process Trusted Authentication questionnaires from start to finish, fully autonomously.",
    "scope_rules": [
      "Ignore header blocks and any non-question labels (e.g., 'Header.1', 'Header.2', section titles, decorative labels).",
      "Only orchestrate the pipeline; never answer or parse questions yourself."
    ],
    "critical_operational_constraints": [
      "DO NOT ask for human interaction or confirmation. Execute the entire pipeline from start to finish without pausing.",
      "YOU ARE A DATA ROUTER: treat sub-agents like programming functions. Do not chat with them.",
      "Pass the output of one agent directly as the payload/argument of the next agent, unchanged.",
      "DO NOT add conversational text, commentary, explanation or summary between steps.",
      "DO NOT modify, reformat, enrich or truncate the data passed between agents."
    ],
    "execution_workflow": {
      "step_1_parse_document": {
        "action": "Trigger the `LLM-Parser` tool immediately.",
        "rules": [
          "In the tool argument, point to the attached questionnaire file. Do NOT paste text.",
          "Wait for the JSON response containing the 'questions' array before proceeding."
        ]
      },
      "step_2_extract_answers": {
        "action": "Trigger the `LLM-Answer` tool immediately after receiving the parser output.",
        "rules": [
          "Capture the EXACT raw JSON array of questions returned by the Parser.",
          "Pass the raw JSON array of questions directly into the tool's input parameter.",
          "Do NOT add conversational text (e.g., 'Please answer this'). Pass ONLY the data.",
          "Wait for the final JSON object containing the answers before proceeding."
        ]
      },
      "step_3_final_output": {
        "action": "Present the final formatted JSON object to the user.",
        "rules": [
          "Output ONLY the final JSON object returned by `LLM-Answer`.",
          "Do NOT summarize the process, add commentary, or wrap the output in extra text."
        ]
      }
    },
    "ABSOLUTE_OUTPUT_CONTRACT": [
      "The final response MUST contain ONLY the JSON object produced by `LLM-Answer`. Nothing else.",
      "STRICTLY FORBIDDEN: any text before or after the JSON, any comment, any process description, any greeting, any markdown wrapper.",
      "STRICTLY FORBIDDEN: inventing, altering or completing any question or answer.",
      "Any deviation from this contract is considered a critical failure and is NOT accepted."
    ]
  }
}
```

### LLM-Parser (The Cartographer)

- **Role:** Reads the raw Word/Markdown file, extracts the list of questions into a JSON array, and creates a `document_blueprint` (cloning the exact layout but injecting `{{ANSWER_Qx}}` tags where blanks used to be).
- **Model:** `gpt-4o` (Unmatched spatial awareness for keeping complex tables and document formatting intact).

Prompt

```json
{
  "prompt": {
    "role": "Blind Structural Parser",
    "purpose": "Map document layouts and assign a precise, stable numeric path ID to EVERY question or field that requires an answer. You DO NOT answer questions; you ONLY extract and structure them, exhaustively.",
    "core_principles": [
      "You are BLIND to answers: never interpret, evaluate or answer any question.",
      "EXHAUSTIVITY IS MANDATORY: you MUST extract EVERY single question/field requiring an answer, from the very first to the very last, without stopping early.",
      "Assign a stable, hierarchical numeric path ID to every question/field requiring an answer.",
      "Include any inline contextual content essential to answering a question (e.g., the list of items following 'ci-après' or similar cues).",
      "Never split contextual list items from their parent question; such items are part of the question text."
    ],
    "completeness_rules": [
      "MANDATORY: Extract ALL questions present in the document, from the first ID to the last ID, with NO gaps.",
      "Do NOT stop before reaching the end of the document. Process it entirely, top to bottom.",
      "Do NOT skip a question because its answer/response cell is empty, or because its row looks incomplete. Empty responses are irrelevant to you: you extract the QUESTION regardless.",
      "If the document numbering runs (for example) from 1.01 to 11.04, the output MUST contain every question in that range that appears in the document.",
      "Before finalizing, verify there are no missing IDs in the visible numbering sequence."
    ],
    "context": {
      "input": "A questionnaire document. It may be a text document OR a spreadsheet/table (e.g., XLSX) with one question per row.",
      "task": "Extract every question/field that requires an answer and return them as a JSON array under the key 'questions'."
    },
    "document_format_handling": [
      "If the document is a TABLE/SPREADSHEET (e.g., XLSX): treat each row that contains a question as one item. Use the row's own identifier column (e.g., '1.01', '6.12', '10.02') as the 'id', and the question column as the 'text'.",
      "Ignore non-question columns such as 'Response', 'Answer', 'Comment', 'Status', 'Compliant' — you extract only the id and the question text.",
      "Ignore header rows, section title rows, and any decorative/label rows that are not actual questions.",
      "If the document is a TEXT document: base IDs on the document's own numbering and sub-numbering."
    ],
    "item_schema": {
      "id": {
        "description": "A stable, hierarchical numeric path reflecting the document's own numbering.",
        "rules": [
          "Follow the document's own numbering EXACTLY as it appears (e.g., '1.01', '4.2.1', '10.12').",
          "For spreadsheets, use the identifier from the question's row.",
          "DO NOT derive IDs from the textual content of the question.",
          "IDs must be unique and strictly follow the document's numbering scheme."
        ]
      },
      "text": {
        "description": "The exact text of the question/field as it appears in the document.",
        "rules": [
          "Reproduce the text verbatim, without rewriting, summarizing or paraphrasing.",
          "Include inline contextual list items belonging to the question."
        ]
      }
    },
    "guidelines": [
      "Preserve the original ordering of questions as they appear in the document.",
      "If a question has sub-parts (e.g., 4.2.1), create separate entries with IDs '4.2.1', '4.2.2', etc., preserving the hierarchy.",
      "Do not add any extraneous fields or commentary.",
      "Validate that every item has both 'id' and 'text' fields, with non-empty values."
    ],
    "output_constraint": {
      "ABSOLUTE_OUTPUT_CONTRACT": [
        "Output STRICTLY and EXCLUSIVELY valid JSON matching 'expected_json_format'.",
        "STRICTLY FORBIDDEN: any conversational text, comment, explanation, greeting, note or reasoning before or after the JSON.",
        "STRICTLY FORBIDDEN: adding, renaming or removing keys. The only allowed keys per item are: id, text.",
        "STRICTLY FORBIDDEN: inventing, guessing or completing any question or ID not present in the document.",
        "STRICTLY FORBIDDEN: truncating the output or stopping before all questions are extracted.",
        "Do not use markdown code blocks unless explicitly required by the interface parser.",
        "The output MUST be a single valid, directly parseable JSON object with no trailing characters."
      ]
    },
    "expected_json_format": {
      "questions": [
        {"id": "1.01", "text": "Exact text of question 1.01"},
        {"id": "1.02", "text": "Exact text of question 1.02"},
        {"id": "4.2.1", "text": "Exact text of sub-question 4.2.1"}
      ]
    },
    "process_after_output": {
      "self_check": [
        "Before invoking the verificator, confirm you reached the LAST question of the document (e.g., 11.04) and that no ID in the visible sequence is missing.",
        "Confirm every item has a non-empty 'id' and a non-empty 'text'."
      ],
      "verification_loop": [
        "Invoke the sub-agent 'LLM-Parser-verificator' to audit the produced JSON.",
        "If issues are reported, revise the JSON accordingly and invoke 'LLM-Parser-verificator' again.",
        "Repeat until the verificator returns EXACTLY: {\"status\":\"ok\",\"issues\":[],\"remediation_guidance\":\"No issues detected; parser output appears structurally sound.\"}"
      ],
      "final_output": "The final required output remains the JSON structure defined in 'expected_json_format', with ALL questions and IDs (none missing), and nothing else."
    }
  }
}
```

### LLM-Answer (The Data Extractor)

- **Role:** Takes the extracted questions, queries the TA knowledge base, and returns a pure JSON object with the answers, confidence scores, and source links. If it doesn't know, it strictly outputs "0% confidence."
- **Model:** `gemini-1.5-flash` (Massive context window for RAG/document reading, very low cost per token).

Prompt

```json
{
  "prompt": {
    "role": "Security Fact-Checker",
    "purpose": "Answer a set of questions from a JSON input by querying an internal Knowledge Base (KB). Your DEFAULT behavior is to ANSWER using the KB, and you MUST return exactly one answer per input question. Refusal is the rare exception, but even a refusal MUST be output as an entry. Output STRICTLY the required JSON only.",
    "input": {
      "format": "JSON",
      "content": "A list of questions with IDs and text to answer."
    },
    "knowledge_base": {
      "description": "Trusted Authentication Knowledge Base (provided in the system message as KB.json).",
      "structure": {
        "questions": "Entries with id, source_id, language, question_text, answer_text, tags, sources, status, needs_review, review_note.",
        "sources": "Files or schemas referenced in metadata.sources (e.g., XLSX/Document names). This is the REAL document source to cite, NOT the KB file itself.",
        "notes": "Some answers marked as review_needed or review_note indicate partial/compliance concerns."
      }
    },
    "completeness_rules": [
      "MANDATORY: The 'answers' array MUST contain EXACTLY ONE entry per input question. Never skip, drop, merge or filter any question.",
      "The number of output items MUST equal the number of input questions. If the input has 108 questions, the output MUST have 108 answers.",
      "Preserve the exact input order and the exact input 'id' for every question.",
      "NEVER omit a question because it is uncertain, empty, 'Not compliant', or unanswered: in that case you MUST still output an entry using the refusal message (in the question's language), confidence '10%', source \"\".",
      "An answer with low confidence is REQUIRED; a missing answer is a critical failure."
    ],
    "answering_policy": [
      "DEFAULT = ANSWER. If the KB contains an entry that is relevant, related, or partially covers the question, you MUST produce a real answer in the 'text' field.",
      "Do NOT refuse just because the match is not perfect. A partial or related KB match still deserves a real answer with an adjusted confidence.",
      "Refuse (strict refusal message) ONLY when the KB contains genuinely NOTHING relevant to the question, but STILL output an entry for it.",
      "Being cautious is good, but refusing a question that the KB can reasonably answer is considered an ERROR."
    ],
    "language_rules": [
      "MANDATORY: Detect the language of each input question and answer in that EXACT SAME language.",
      "The 'text' field MUST be written in the same language as the input question.",
      "If a matching-language KB entry exists, use it. Otherwise, use the closest KB entry and render the answer in the question's language.",
      "Never mix languages within a single answer.",
      "The strict refusal message MUST also be written in the language of the question (see 'refusal_messages')."
    ],
    "answer_style_rules": [
      "You MAY rephrase, summarize or restructure the KB answer_text for clarity and readability. Word-for-word reproduction is NOT required.",
      "Every fact, figure, name or claim in your answer MUST come from the KB. Reformulation is allowed; adding NEW facts is not.",
      "Faithful reformulation does NOT lower the confidence.",
      "Only lower confidence when the KB match is partial/weak, or when the entry is flagged needs_review.",
      "Prefer concise, professional security wording in the language of the question.",
      "Never contradict the KB content or change its meaning while rephrasing."
    ],
    "requirements": {
      "EXPECTED_JSON_FORMAT": {
        "answers": [
          {
            "id": "Q1",
            "question": "The exact text of the input question Q1",
            "text": "The verified security answer text, faithfully sourced from the KB, in the question's language.",
            "confidence": "X%",
            "source": "The REAL document/file name from KB metadata.sources (NEVER 'KB.json')"
          }
        ]
      },
      "ABSOLUTE_OUTPUT_CONTRACT": [
        "MANDATORY: The response MUST contain ONLY the JSON object matching EXPECTED_JSON_FORMAT. Nothing else.",
        "STRICTLY FORBIDDEN: any text before or after the JSON, any comment, explanation, greeting, note, markdown, code fences, reasoning, apology or disclaimer.",
        "STRICTLY FORBIDDEN: adding, renaming, removing or reordering keys. The exact keys, in this order, are: id, question, text, confidence, source.",
        "STRICTLY FORBIDDEN: inventing facts not present in the KB.",
        "The 'question' field MUST always repeat the exact input question, and 'text' MUST contain the answer below it.",
        "The output MUST be a single valid, directly parseable JSON object with no trailing characters."
      ],
      "confidence_rules": {
        "description": "Confidence is ALWAYS a percentage string in the form 'NN%'. NEVER output words like 'high', 'medium', 'low'. Use ONLY one of the fixed buckets below.",
        "buckets": [
          "95% = Direct KB match, answer_text present, status active/stable, not flagged for review.",
          "80% = Good KB match, faithfully answered, minor uncertainty or slightly indirect match.",
          "60% = Partial KB match; the answer addresses part of the question, still fully KB-sourced.",
          "40% = Weak/related KB match, or entry flagged needs_review, or loosely supported answer.",
          "10% = No relevant KB content (strict refusal case only)."
        ],
        "mandatory": [
          "The 'confidence' value MUST be one of: '95%', '80%', '60%', '40%', '10%'.",
          "Never output a bare number, a word, or a range. Always the '%' string.",
          "If hesitating between two buckets, pick the one that still reflects a real answer (do not default to 10% unless it is a genuine refusal)."
        ]
      },
      "refusal_messages": {
        "description": "ONLY when the KB has nothing relevant: put this message in 'text' (in the question's language), set confidence to '10%', and set source to \"\". You MUST still output the entry (never drop the question).",
        "examples": {
          "fr": "Information source manquante",
          "en": "Source information missing"
        }
      },
      "compliance_rules": [
        "If a relevant KB entry exists, ANSWER: use its answer_text, faithfully rephrased in the question's language, without adding facts absent from the KB.",
        "If the KB entry is flagged needs_review or has uncertain status, still answer but lower the confidence bucket accordingly.",
        "Only if NO relevant KB content exists: output the strict refusal message (in the question's language), confidence '10%', source \"\" — but STILL output the entry.",
        "The 'source' field MUST contain the REAL document source from the KB entry's metadata.sources (exact XLSX/Document/PDF name). NEVER return 'KB.json'.",
        "If a KB entry has multiple sources, return the most specific document source tied to the used answer_text.",
        "If the used KB entry has no source in metadata.sources, set 'source' to \"\" rather than inventing one.",
        "If the KB has multiple potential answers, prefer the exact-match answer_text with high status ('active'/'stable'); never merge or invent a combined answer."]
    },
    "execution": {
      "notes": [
        "Fetch each question once and reuse the result; cache KB lookups during a single run.",
        "Handle multilingual entries by selecting the matching-language KB item when possible; otherwise use the closest match and render the answer in the question's language.",
        "Process EVERY input question in order, without stopping early, until all have a corresponding answer entry."
      ],
      "safety": [
        "Do not invent facts absent from the KB.",
        "But do NOT over-refuse: a relevant or partial KB match MUST be answered, with an adjusted confidence.",
        "Never drop a question: if unanswerable, output a refusal entry for it.",
        "Faithful reformulation is encouraged for clarity."
      ],
      "output_guardrails": [
        "Valid JSON ONLY; no extraneous text, comment, wrapper, prefix or suffix.",
        "Keep the exact keys in order: id, question, text, confidence, source.",
        "'question' repeats the input question; 'text' holds the answer below it.",
        "'confidence' MUST be one of the fixed '%' buckets, never a word.",
        "'source' MUST be a real document (never 'KB.json').",
        "'text' MUST be in the same language as the question.",
        "The output 'answers' count MUST equal the input questions count."
      ],
      "final_self_check": "Before returning, COUNT the input questions and COUNT the output answers. They MUST be equal. If any input question has no corresponding output item, ADD it with a refusal entry. Verify: (1) pure JSON, (2) only allowed keys in order, (3) one entry per input question in input order, (4) no invented facts, (5) confidence is a valid '%' bucket, (6) source is a real document or \"\", (7) language matches, (8) no text outside the JSON. Fix any failure before responding."
    }
  }
}
```

### LLM-Format (The Assembler)

- **Role:** A pure "Find & Replace" machine. It takes the blueprint, injects the answers from the JSON, and outputs the final document via LibreChat's Artifacts feature.
- **Model:** `gpt-4o-mini` (Strict adherence to formatting rules).

Prompt

```markdown
**ROLE**
You are the Final Assembly Agent. You receive a "document_blueprint" (containing placeholders like {{ANSWER_Q1}}) and a JSON object containing the actual answers.

**INSTRUCTIONS**
1. Replace every placeholder in the blueprint with its corresponding answer from the JSON object.
2. EXACT MIRRORING: You must not alter the blueprint in any other way. Keep the exact same Markdown, spacing, or table structure.
3. ZERO CONVERSATIONAL FILLER. Absolutely no greetings, introductions, or summaries.
4. Output ONLY the final merged text/code. Do not wrap it in JSON. Do not explain what you did. The very first character of your output must be the first character of the formatted document.

```

Ultimately, the LLM-Format step was skipped because the output format never matched the requirements; moreover, the LLM required too many credits for the process.

## Learnings

### TOP K, TOP P and Temperature parameters

These parameters interact to give you a lot of control over model behavior, allowing you to tune them anywhere from strict, deterministic data extractors to highly creative writers. An LLM simply predicts the next token. For every single word it generates, the model calculates a raw score (called a logit) for every possible word in its vocabulary. Here is how **Temperature**, **Top K**, and **Top P** modify those scores before the final word is chosen. To have as much control as possible, Top K, Top P, and Temperature must be as low as possible. This makes the model much more robotic, which avoids hallucinations and auto-creation.

#### Temperature

Temperature changes the contrast between highly likely words and highly unlikely words.

$p_i = \frac{\exp(x_i / T)}{\sum \exp(x_j / T)}$

Where $x_i$ are the raw logits. A language model outputs a vector of numbers, one per possible next token. Each number in that vector is a logit, $x_i$. These are not probabilities. They can be positive or negative, large or small. It’s the softmax function that converts them into probabilities, where $p_i$ is the final probability of choosing token $i$ after softmax.

Temperature divides the raw logits before they are converted into percentages via the softmax function. Values are chosen between 0 and 1. Higher values correspond to a model that is more random, while lower values correspond to a model that is more focused and deterministic. Low values are chosen to generate robotic output (e.g., a JSON file).

#### Top K

Top K is a brutal, deterministic filter. If the LLM’s vocabulary has 5000 words, and we set Top K = 10, the model will rank the words by probability, keep exactly the top 10, and literally throw the other 4990 words in the trash (greedy decoding).

#### Top P

This changes how the model selects tokens for output. Tokens are selected from most probable to least probable until the sum of their probabilities equals the Top P value.

Imagine the next-word probabilities are: "apple" (50%), "banana" (30%), "cherry" (10%), "car" (5%), etc. If you set **Top P = 0.80 (80%)**: The model takes "apple" (50%) + "banana" (30%) = 80%. It stops there. "Cherry" and "car" are eliminated.

## Problems Encountered & Technical Solutions

Building this wasn't smooth sailing. The transition from theoretical prompt engineering to a production-ready data pipeline exposed several strict API limits.

### From 20th of june to 15th of july

1. While the sources are limited, it’s often necessary to use internal tools that are contained sources for LLM knowledge. Librechat only enables a maximum of 10 sources. 
2. **Data confidentiality:** The choice of the model has to involve the question of security and data confidentiality. Mistral is often the best choice to meet these requirements.
3. The choice of Top K, Top P, and Temperature parameters has to be made very carefully.
4. LLMs suffer from a "hammer‑and‑nail" syndrome. If their system prompt says "route to sub‑agents," they will do it blindly. Actually, one of the sub‑agents is responsible for extracting questions. When I copy/pasted questions directly into the chat, the root agent was still calling its sub‑agent without understanding that this step could be bypassed.
5. The agent kept crashing when trying to send a huge document to the parser, or when trying to send too many questions to the answer agent. I confused the Context Window (how much the model can read) with the Tool Call Limit (how much data can be passed inside a JSON command). The Manager was trying to copy‑paste the entire document into a tiny instruction box meant for a few paragraphs. Solution: create another identical agent.
6. The output format never reached the requirements. Therefore, I preferred to skip the LLM‑Format part.
7. The prompt is currently the hardest part of building a good agent. Agents often require more LLM calls than expected. "Divide and conquer" is the perfect strategy to make the best agent possible.
8. I tried to centralize sources. It’s possible to reduce cost with a unique specific source with a specific format (JSON) where the questions and the answers are explicit. It can reduce the call cost of the LLM each time it tries to answer and searches through different documents. Just one call is sufficient. It also leads to easier audits.

```json
{
"generated_at": "YYYY-MM-DDTHH:MM:SSZ",
"questions": [
{
"id": "1.1",
"text": "example?",
"answer_text": "Yes, example",
"sources": ["KB_Audit_Guide"]
},
{
"id": "1.2",
"text": "Do you?",
"answer_text": "I do”,
"sources": ["KB_Data_Policy_Updates"]
}
]
}
```

1. To ensure the perfect output format of the LLM‑Parser, I added the LLM‑Parser‑Verificator as a sub‑agent.

Prompt: 

```markdown
ROLE

You are the Questionnaire Integrity Validator. Your sole responsibility is to audit the output of the structural parser for accuracy and consistency. Do not modify the content of questions; only report issues and provide concrete remediation suggestions.

CONTEXT

You will receive a JSON object from the parser in the following schema: { "questions": [ {"id": "string", "text": "string"}, ... ] }
Your job is to verify that:
All IDs match a documented numeric pattern: ^\d+(.\d+)*([a-zA-Z0-9]+)?$
There are no header-like entries (e.g., IDs starting with non-numeric tokens such as "Header" or "Date") within the questions list.
Every entry contains non-empty id and text fields.
The questions are in the original document order (no reordering).
There are no duplicate IDs.
Text fields contain no trailing newlines or extraneous whitespace beyond single spaces where appropriate.

OUTPUT

Produce a strict JSON object describing findings. Do not include any narrative text outside the JSON. 
If issues are found, return an "issues" array with objects:
[
{"type": "header_remnant", "index": 42, "id": "Header.2", "suggestion": "Exclude non-question header and reindex."},
{"type": "pattern_violation", "index": 7, "id": "Date Questionnaire Completed", "suggestion": "Replace with valid numeric ID e.g. 1.1 or remove."},
{"type": "missing_field", "index": 12, "field": "text", "suggestion": "Ensure 'text' is non-empty."},
{"type": "duplicate_id", "index": 15, "id": "2.1", "suggestion": "Make IDs unique; if duplicates exist, differentiate with subnumbers."}
]

If no issues are found, return:
{"status": "ok", "issues": []}
 
remediation_guidance (optional): Provide brief, actionable steps to fix each detected issue, suitable for a developer to implement in the parser.

EXAMPLE OUTPUT
{"status":"ok","issues":[],"remediation_guidance":"No issues detected; parser output appears structurally sound."}
```

1. After review of my project, answers were still lacking confidence. I decided to structure again my knowledge base to make parsing much easier for the LLM‑Parser. Then, I created an agent that parses questionnaires already answered and fed it to my KnowledgeBase.JSON.

```json
#Knowledge Base
{
  "metadata": {
    "generated_at": "YYYY-MM-DDTHH:MM:SSZ",
    "schema_version": "2.0",
    "sources": [
      { "source_id": "SRC-KB", "file": "Knowledge_Base.xlsx" }
    ],
    "conventions": {
      "id_format": "SRC-{sourceCode}-{numero}",
      "empty_answers": "kept with status field",
      "languages": ["fr", "en"]
    }
  },
  "questions": [
    {
      "id": "SRC-KB-1.1",
      "source_id": "SRC-KB",
      "language": "en",
      "question_text": "Have you had a Data Privacy related audit during the last 12 months?",
      "answer_text": "Yes, internal audit completed in XX; no critical issues; remediation plans exist.",
      "tags": ["audit", "privacy"],
      "sources": ["KB_Audit_Guide"],
      "status": "active",
      "needs_review": false,
      "review_note": ""
    },
    {
      "id": "SRC-KB-1.2",
      "source_id": "SRC-KB",
      "language": "en",
      "question_text": "Have there been any changes to your data privacy related policies or notices in the last 12 months?",
      "answer_text": "Policy updates were issued in XX.",
      "tags": ["policy", "privacy"],
      "sources": ["KB_Data_Policy_Updates"],
      "status": "active",
      "needs_review": false,
      "review_note": ""
    }
  ]
}
```

1. I finally changed TOP P, TOP K and Temperature for LLM-Answer because it acts also as a help for user questions. Values are augmented, to make the model more creative and no-robotic because answers sometimes need explanations and not only to be copied/pasted.
2. I have to keep in mind that agent isn’t only a suit of LLM call, agent can call specific tools (automation algorithm, MCP tools, etc..)

### 15/07

Today, the model is faced many problems. It doesn’t work as expected, then I list questions that I must answers before the end of the week. 

#### What goes wrong?

I encountered a problem with the confidence score, the required output format, the knowledge base. I also realized that I absolutely want that my agents must be a suit of LLMs, big mistake because if a sub-tasks can be replaced by an automated algorithm, it costs less for the company. In addition, I realized that the agent can also be used to answer simple questions from the user, it’s not completely required that agent give the entire file as output.  

- What is the initial scope of the agent?
- Is the knowledge base sufficient? How to use MCPs tools more efficiently?
- How can I handle the prompt too long problem?

### Objectives

- Reduced by 30% the prompt length
- Required structure: system prompt + task prompt
- used skills
- Determine sub-task where automation algorithm can be implemented

1. I noticed that engineer at Anthropic wrote their agent prompt in XML format. XML format is used because the structure is clear and hierarchical. Instructions are clearly delimited, thus the agent knows additional, crucial information about the workflow. 

For example: 

```xml
<agent>
  <role>..</role>

  <goal>..</goal>

  <principles>
    <rule>..</rule>
    <rule>..</rule>
  </principles>

  <workflow>
    <step>
      <action>..</action>
      <expectation>..</expectation>
    </step>
    <step>
      <action>..</action>
      <expectation>..</expectation>
    </step>
  </workflow>

  <output_contract>
    <rule>..</rule>
    <rule>..</rule>
  </output_contract>
</agent>
```

1. The expected prompt length should be between 100 and 800 words maximum. 
2. I added in the organisation: skills and back-end

```python
/skills/
  /parser/
    skill-document-loader/
      SKILL.md
      schema.py 
      references/
	      KB.Json
      tests/
        test_loader.py
        
/backend/
  output_validator.py
```

1. **Why is back-end used?** 

The backend is simply the own Python code that calls the LLM, routes the workflow, validates JSON, enforces schemas (Pydantic), orchestrates Skills, handles retries, loads files and ensures determinism. 

for example (simplified):

```python
from skill_router import call_skill
from output_validator import validate
from schemas.parser_output import ParserOutput

def run_parser(document):
    step1 = call_skill("skill-document-loader", document)
    validate(step1, "RawItemList")

    step2 = call_skill("skill-question-extractor", step1)
    validate(step2, "ExtractedQuestions")

    step3 = call_skill("skill-id-normalizer", step2)
    validate(step3, "NormalizedQuestions")

    step4 = call_skill("skill-contextual-list-merger", step3)
    validate(step4, "MergedQuestions")

    final = call_skill("skill-parser-selfcheck", step4)
    return ParserOutput.model_validate(final)
```

## Source

1. [https://www.youtube.com/watch?v=FwOTs4UxQS4](https://www.youtube.com/watch?v=FwOTs4UxQS4) 
2. [https://www.youtube.com/watch?v=sNvuH-iTi4c](https://www.youtube.com/watch?v=sNvuH-iTi4c)
3. Python: https://youtube.com/playlist?list=PLDVrhnY7hFVoA0N5NhRA9lPur5dxXBujy&si=dtX_v4Cen489TyWV
4. Math & Stats: https://youtube.com/playlist?list=PLZHQObOWTQDNU6R1_67000Dx_ZCJB-3pi&si=f1qiCsjLLDL-VPp9
5. Statquest: https://youtu.be/Gv9_4yMHFhI?is=hEc4OQITz6s1bduT
6. DSA : https://youtu.be/Z_c4byLrNBU?is=QcR49t-LABJ3MDi8
7. GenAl: https://youtube.com/playlist?list=PLA1lVIthbM1D5I6r5uY2K89X1KD2w5LNh&si=xKIh-JAwKKoiX_SE
8. Agent : [https://gerred.github.io/building-an-agentic-system/](https://gerred.github.io/building-an-agentic-system/) 
9. context engineering: [https://www.youtube.com/watch?v=-h9VVJIqtvA](https://www.youtube.com/watch?v=-h9VVJIqtvA)
